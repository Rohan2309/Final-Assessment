</div>

<style>
  /* Soft Pastel Alert Styling */
  .alert {
    border-radius: 12px;
    padding: 12px 18px;
    font-weight: 600;
    box-shadow: 0 3px 12px rgba(0,0,0,0.06);
    border: none !important;
    opacity: 0.95;
    transition: opacity .3s ease, transform .3s ease;
  }

  .alert-success {
    background: #bbf7d0; /* pastel green */
    color: #065f46;
  }

  .alert-error, 
  .alert-danger {
    background: #fecaca; /* pastel red */
    color: #7f1d1d;
  }

  .alert-warning {
    background: #fde68a; /* pastel yellow */
    color: #78350f;
  }

  .alert-info {
    background: #bfdbfe; /* pastel blue */
    color: #1e3a8a;
  }

  /* Fade Animation */
  .alert.fade {
    opacity: 0;
    transform: translateY(-5px);
  }

  footer {
    margin-top: 40px;
    padding: 20px 10px;
    text-align: center;
    color: #64748b;
    font-size: 0.9rem;
  }
</style>

<!-- BOOTSTRAP JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<script>
  // Auto-hide alerts with smooth fade
  document.addEventListener("DOMContentLoaded", function () {
    const alerts = document.querySelectorAll(".alert");
    
    alerts.forEach((alert) => {
      setTimeout(() => {
        alert.classList.add("fade");
        alert.classList.remove("show");

        setTimeout(() => alert.remove(), 500);
      }, 2000);
    });
  });
</script>

<footer>
  © <%= new Date().getFullYear() %> Machine Test • Designed with pastel UI 🌸
</footer>

</body>
</html>
